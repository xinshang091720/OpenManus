### 2026-07-30

- 任务列表 ItemsControl 改为 ListBox：添加 SelectedItem="{Binding SelectedGroupHistory}"、自定义 ItemContainerStyle（悬停#F0F0F5、选中#E8F0FE、圆角6）
- VM 新增 SelectedGroupHistory 属性，setter 中自动触发 LoadChatHistoryAsync
- 新增历史对话接口 SelectChatHistory（POST /ai/chatHistory/list），Header 用 HttpRequestMessage 请求级
- 新增 ChatHistoryItem / ChatHistoryListResponse 模型
- 选中任务项时：清除 Messages/_history → 调 API → 按 role(user/assistant) 填充 ChatMessage 列表 → 同步 HistoryMessage
- AI 回复头部新增 SidebarSimple 图标 + "AI智建" 文字
- 侧边栏任务列表改为树形结构：新增 GroupHistoryNode 模型（IsGroup/IsExpanded/Children），按 SavePath 分组聚合，XAML 用 ItemsControl 嵌套实现分组头（ToggleButton 折叠展开）和叶子节点两种布局
- HomeInterfaceVM 加 partial 修饰符（[RelayCommand] 需要）
- **重新实现树形分组结构（改进版）**：
  - Model: GroupHistoryNode 继承 ObservableObject，[ObservableProperty] IsExpanded 支持双向绑定
  - VM: 新增 SelectGroupItemCommand（Command 方式，重复点击也能触发），AddGroupHistories 按 SavePath 分组构建
  - XAML: ItemsControl 嵌套，分组头（ToggleButton + 文件夹图标 + 箭头），子项/叶子节点（Border + MouseLeftButtonDown → code-behind 解析 DataContext → 调用 SelectGroupItemCommand）
  - Code-behind: GroupHistoryItem_Click 统一处理子项（GroupHistory）和叶子节点（GroupHistoryNode.Item）的点击
  - **RuntimeManager.GetExePath 简化**：只找程序同目录 BeeSync.AgentRuntime\BeeSync.AgentRuntime.exe，删除 4 条回退路径（包括桌面/AISmartConstructionProject 路径），异常信息包含期望全路径
  - **RuntimeManager 构造函数清理**：删除无用的 exe 查找日志逻辑，删除 RuntimeExeDir/RuntimeExePath 静态字段
  - **RuntimeManager 残留进程清理恢复**：StartAsync 开头取消注释 Process.GetProcessesByName("BeeSync.AgentRuntime") Kill 逻辑
  - **RevitProxyService 端口冲突修复**：HttpListenerException 捕获 → netstat -ano 查找占用 PID → Kill → 重试，新增 TryKillPortOccupant 方法
  - **RevitProxyService ProxyPort 50000→5000**
  - **App.xaml.cs** 静态构造函数注册 Encoding.RegisterProvider(CodePagesEncodingProvider.Instance) 解决 GBK 编码不可用
  - **RevitProxyService TryKillPortOccupant 重构**：拆分为 KillPortProcess（netstat 杀进程，排除 PID 4/System）+ ClearHttpSysReservation（netsh http delete urlacl 清理 http.sys 残留 URL 绑定），Start() 重试前 Thread.Sleep(500) 等待内核层释放
  - **App.xaml.cs 全局异常捕获**：DispatcherUnhandledException + AppDomain.UnhandledException + TaskScheduler.UnobservedTaskException，所有异常写日志防静默闪退
  - **HomeInterfaceVM.AddGroupHistories 加 try-catch**：async void 方法异常直接炸进程，是另外一台电脑闪退根因
  - **AccountApiClient 全部 13 个接口加 try-catch**：统一模式（初始化 result → try 内赋值 → catch 记日志 → return result），API 调用失败不再抛异常到上层
