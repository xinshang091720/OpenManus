# 2026-07-28

## 重建登录注册界面（第一版：粗略风格）
- 源文件 `View/LoginRegistration/Login.xaml(.cs)` 和 `Register.xaml(.cs)` 已被删除且无法恢复（无 git 历史、无回收站、无临时备份）
- 根据 `obj/Debug` 残留的 `.g.i.cs` 生成文件反推结构，重建了 4 个文件：
  - `View/LoginRegistration/Login.xaml` — 登录窗口，无边框圆角卡片风格，含用户名/密码输入、错误提示、切换注册链接
  - `View/LoginRegistration/Login.xaml.cs` — PasswordBox 手动同步、Hyperlink 跳转注册页、临时直通主界面（待对接后端 API）
  - `View/LoginRegistration/Register.xaml` — 注册窗口，含用户名/密码/确认密码、前端校验、切换登录链接
  - `View/LoginRegistration/Register.xaml.cs` — 两次密码一致性校验、临时注册成功跳转登录
- 风格匹配项目：HandyControl + 灰色系 (#303133 按钮, #F5F7FA 输入框背景, #F56C6C 错误提示)
- 清理了 `obj/Debug` 中 3 个旧的 `.g.i.cs` 残留文件
- 修改 `App.xaml` StartupUri: `HomeInterface.xaml` → `Login.xaml`
- 编译通过：0 错误
- 登录/注册按钮的 API 调用部分以 TODO 注释留空，当前为临时直通逻辑

## 重建登录注册界面（第二版：根据截图精确还原）
- 用户提供截图，显示原始登录界面包含：
  - 标题栏"登录"+关闭按钮
  - Tab切换：短信登录 / 密码登录（蓝色下划线指示器）
  - 密码登录：手机号+图标、密码+图标、邀请码+图标、申请授权链接
  - 短信登录：手机号+图标、验证码+获取验证码按钮、邀请码+图标、申请授权链接
  - 蓝色登录按钮（#4080FF）
  - 勾选框：已阅读并同意《用户协议》和《隐私政策》
  - 底部：联系我们 / 没有账号？去注册
- 完全重写 4 个文件：
  - `Login.xaml` — 精确还原截图风格：白色卡片、圆角8、灰色边框输入框、内嵌图标（Emoji）、底部蓝线Tab指示器
  - `Login.xaml.cs` — 支持短信/密码登录切换，前端校验（手机号11位、密码非空、勾选协议），TODO注释留空API调用
  - `Register.xaml` — 同样风格：手机号、验证码+获取按钮、密码、确认密码、邀请码、申请授权、蓝色注册按钮、协议勾选、去登录链接
  - `Register.xaml.cs` — 前端校验（手机号、验证码、密码长度、两次密码一致、勾选协议），注册成功跳转登录
- 修复编译问题：
  - WPF Style中Storyboard.TargetName不能在Setter中使用 → 改为Border+MouseLeftButtonDown手动切换Tab状态
  - 移除async void的GetCodeBtn_Click中的async关键字（临时空实现）
- 编译通过：0 错误，2 个原有warning（HomeInterface.xaml.cs nullable）
- 输入框使用Grid内嵌TextBlock（Emoji图标）+TextBox/PasswordBox方式，无HandyControl依赖
- 所有超链接（申请授权、用户协议、隐私政策）均以TODO留空，待后续实现
