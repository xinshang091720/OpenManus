# 2026-07-29

## 对接获取验证码接口
- 接口地址：`POST http://192.168.1.71:9001/api/account/verifyCode/sendCaptcha`
- 请求参数：`phone` (string), `codeType` (string, default "0")
- 响应：`{ code, msg }`，`code == 200` 为成功

### 新增/修改文件
- `Model/ApiModels.cs` — 新增 `SendCaptchaRequest`、`BaseResponse`、`SendCaptchaResponse`
- `Api/AccountApiClient.cs` — 新建账号服务 API 客户端，独立 BaseUrl `192.168.1.71:9001`，含 `SendCaptchaAsync` 方法
- `ViewModel/LoginRegistrationVM/LoginVM.cs` — 完全重写：
  - 属性：Phone、SmsCode、Password、InviteCode、IsAgreed、ErrorMessage、CaptchaButtonText、IsCaptchaEnabled、IsLoginEnabled
  - 命令：GetCaptchaCommand（60秒倒计时，调用 AccountApiClient）、LoginCommand（前端校验 + 临时直通主界面）、CloseWindowCommand、GoToRegisterCommand
  - 注入 `Func<string>? GetPasswordFunc` 解决 PasswordBox 无法直接绑定的问题
- `View/LoginRegistration/Login.xaml` — 添加 `vm:LoginVM` DataContext，所有输入框绑定对应属性，获取验证码按钮绑定 `GetCaptchaCommand` + `CaptchaButtonText` + `IsCaptchaEnabled`，登录按钮绑定 `LoginCommand`，协议勾选绑定 `IsAgreed`，错误提示 TextBlock 绑定 `ErrorMessage`，去注册按钮绑定 `GoToRegisterCommand`
- `View/LoginRegistration/Login.xaml.cs` — 注入 `GetPasswordFunc` 委托，通过 `FindName("PwdBox")` 获取 HandyControl PasswordBox 的 Password

### 待对接
- 短信登录 API
- 密码登录 API
- 注册 API
- 当前登录/注册成功后的逻辑为临时直通主界面（OpenMainWindow）

## 对接密码登录接口
- 接口地址：`POST http://192.168.1.71:9001/api/account/login`
- 请求参数：`username` (string), `password` (string), `deviceNo` (string), `portFlag` (string, 固定 "pluginDesktop")
- 响应：`{ msg, code, data: { authorise, userId, token } }`，`code == 200` 为成功

### 新增/修改文件
- `Model/ApiModels.cs` — 新增 `LoginRequest`、`LoginData`、`LoginResponse`
- `Api/AccountApiClient.cs` — 新增 `LoginAsync` 方法
- `ViewModel/LoginRegistrationVM/LoginVM.cs` — `LoginAsync` 密码登录分支替换 TODO：
  - 调用 `_accountApi.LoginAsync(Phone, pwd, deviceNo)`
  - 校验响应：成功则保存 Token → `ApiConfig.AccountToken`
  - 保存 `UserId`、`Authorise` → `ApiConfig`
  - 失败显示错误信息，成功跳转主界面
  - 新增 `GetDeviceNo()` 方法获取机器名作为设备标识
- `Api/ApiConfig.cs` — 新增账号服务字段：`AccountToken`、`UserId`、`Authorise`、`IsLoggedIn`

### 编译结果：0 错误，2 警告（均为 HomeInterface.xaml.cs 的 nullable 警告，无关）

## 对接短信验证码登录接口
- 接口地址：`POST http://192.168.1.71:9001/api/account/verifyCode/captchaLogin`
- 请求参数：`userName` (string), `code` (string), `deviceNo` (string), `codeType` (string, 固定 "0")
- 响应格式与密码登录一致（共用 `LoginResponse`）

### 新增/修改文件
- `Model/ApiModels.cs` — 新增 `CaptchaLoginRequest`
- `Api/AccountApiClient.cs` — 新增 `CaptchaLoginAsync(phone, code, deviceNo, codeType)` 方法
- `ViewModel/LoginRegistrationVM/LoginVM.cs` — 重构 `LoginAsync`：
  - 提取 `HandleLoginResponse` 通用处理方法（保存 Token、跳转主界面）
  - 短信登录 Tab 调用 `_accountApi.CaptchaLoginAsync(Phone, SmsCode, deviceNo)`
  - 密码登录 Tab 调用 `_accountApi.LoginAsync(Phone, pwd, deviceNo)`
  - 统一异常捕获、错误提示

### 待对接
- 注册 API

### 编译结果：0 错误，2 警告

## 登录信息加密保存到本地 JSON 文件
- 程序同目录保存 `login.dat`（加密的 JSON 文件）
- 加密方式：DPAPI (`ProtectedData.Protect`, `DataProtectionScope.CurrentUser`)，仅本机本用户可解密

### 新增/修改文件
- `AiConstruction.csproj` — 新增 NuGet 包 `System.Security.Cryptography.ProtectedData` v9.0.11
- `Api/ApiConfig.cs` — 新增：
  - `LoginDataPath` 属性（程序目录 + login.dat）
  - `EncryptString(string plainText)` → Base64 密文
  - `DecryptString(string cipherBase64)` → 明文
  - 使用 `System.Security.Cryptography.ProtectedData`（DPAPI）
- `Model/ApiModels.cs` — 新增 `SavedLoginInfo` 模型（AccountToken/UserId/Authorise/Phone/Password）
- `ViewModel/LoginRegistrationVM/LoginVM.cs`：
  - `HandleLoginResponse` 末尾调用 `SaveLoginInfo()`
  - 新增 `SaveLoginInfo()` 方法：构建 `SavedLoginInfo` → JSON 序列化 → `EncryptString` 加密 → 写入 `login.dat`
  - 补回缺失的 `ErrorMessage` `[ObservableProperty]`
  - 新增 `using System.Text.Json`

### 编译结果：0 错误，2 警告

## 修复未注册弹窗遮罩层
- 修复 LoginAsync 开头硬编码弹窗问题（删除无判断条件的弹窗代码）
- Login.xaml 新增全屏遮罩层 MaskLayer（`#80000000`，默认 Collapsed）
- LoginVM 新增 `SetMaskVisibilityAction` 委托，HandleLoginResponse 中弹窗前后控制遮罩层
- Login.xaml.cs 注入 SetMaskVisibilityAction
- Unregistered.xaml 改为透明背景 + CenterOwner

## 完善 RegisterVM
- `ViewModel/LoginRegistrationVM/RegisterVM.cs` — 完善：
  - 属性：NickName、Phone、SmsCode、Email、Company、Position、PositionList（14个建筑行业职位）、IsAgreed、CaptchaButtonText、IsCaptchaEnabled、IsRegisterEnabled
  - 委托：GetPasswordFunc、GetConfirmPasswordFunc（PasswordBox 绑定）
  - 命令：GetCaptchaCommand（60秒倒计时）、RegisterCommand（含完整校验：昵称→手机号→验证码→密码强度≥8位+大小写+数字+特殊字符→确认密码一致→公司→职位→协议）、CloseCommand
  - 注册成功后跳转登录窗口
