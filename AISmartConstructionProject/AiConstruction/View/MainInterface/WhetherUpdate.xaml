<Window x:Class="AiConstruction.View.WhetherUpdate"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AiConstruction.View"
        xmlns:vm="clr-namespace:AiConstruction.ViewModel"
        mc:Ignorable="d"
        Title="更新提示"
        xmlns:viewmodel="clr-namespace:AiConstruction.ViewModel"
        d:DataContext="{d:DesignInstance Type=viewmodel:WhetherUpdateVM}"
        Height="188"
        Width="458"
        Topmost="True"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None">
    <Border Background="White"
            CornerRadius="8"
            Width="458"
            Height="188"
            VerticalAlignment="Center"
            HorizontalAlignment="Center">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="48" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0"
                  Background="Transparent">
                <TextBlock Text="提示"
                           FontSize="16"
                           Foreground="#303133"
                           VerticalAlignment="Center"
                           Margin="24,0,0,0" />
                <Button Style="{StaticResource CloseBtnStyle}"
                        Command="{Binding CloseCommand}"
                        Margin="0 0 10  0 "
                        HorizontalAlignment="Right"
                        CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor,AncestorType={x:Type Window}}}" />
            </Grid>
            <StackPanel Orientation="Vertical"
                        Grid.Row="1"
                        Margin="18 20 18 0">
              
                <TextBlock Text="{Binding Content}"
                           Foreground="#606266"
                           Margin="0 20 0 0"
                           TextWrapping="Wrap"
                           FontSize="16"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center" />

                <StackPanel Orientation="Horizontal" Margin="0 30  0 0" HorizontalAlignment="Right">
                    <Button Content="取消"
                            Width="62"
                            CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor,AncestorType={x:Type Window}}}"
                            Command="{Binding CloseCommand}" />
                    <Button Content="确认"
                            Margin="15 0 0 0"
                            Width="62"
                            Command="{Binding ConfirmCommand}"
                            CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor,AncestorType={x:Type Window}}}" />
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>
