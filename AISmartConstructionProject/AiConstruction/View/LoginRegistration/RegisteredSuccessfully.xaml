<Window x:Class="AiConstruction.View.RegisteredSuccessfully"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AiConstruction.View"
        mc:Ignorable="d"
        Title="提示"
        xmlns:viewmodel="clr-namespace:AiConstruction.ViewModel"
        Height="236"
        Width="360"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None">
    <Border Background="White"
            CornerRadius="8"
            Width="360"
            Height="236"
            VerticalAlignment="Center"
            HorizontalAlignment="Center">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="48" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0"
                  Background="Transparent">
                
                <TextBlock Text="提示"
                           FontSize="16"
                           Foreground="#303133"
                           VerticalAlignment="Center"
                           Margin="24,0,0,0" />
                <Button Style="{StaticResource CloseBtnStyle}"
                        Command="{Binding CloseWindowCommand}"
                        Margin="0 0 10  0 "
                        Click="Button_Click"
                        HorizontalAlignment="Right"
                        CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor,AncestorType={x:Type Window}}}" />
            </Grid>
            <StackPanel Orientation="Vertical"
                        Grid.Row="1"
                        Margin="20 39 20 0">
                <Image Source="pack://application:,,,/AiConstruction;component/img/CheckCircle.png"
                       Width="36"
                       Height="36"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"/>
                <TextBlock Text="注册成功"
                           Foreground="#606266"
                           FontSize="16"
                           Margin="0 12 0 0"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center" />
                <TextBlock Text="请耐心等待工作人员授权使用"
                           Foreground="#909399"
                           FontSize="16"
                           Margin="0 39 0 0"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
