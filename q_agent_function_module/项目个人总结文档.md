
第一个    基点是从py这边根据dwg图纸获取基点然后打开对应的一个项目的模型调用C#修改基点的接口完成对reivt模型的基点的修改

第二个   调用C#的BatchCreateRooms接口可以批量创建房间（创建房间需要有房间的特征  所以只有建筑专业的模型才需要创建房间ST-结构  AC-通风空调   PD-给排水  EL-电气等不需要执行下面的任务了   其次我再解释一下第二步以及第三步主要是为了把reivt封闭空间的房间名称替换成dwg图纸中实际的房间名称）

第三步   调用C#的接口返回计算平移量的reivt轴网中心点坐标和dwg轴网的中心点坐标    py这边再计算两个轴网的偏移量     偏移量拿到之后去获取图纸里面的所有房间坐标   然后对所有的房间进行偏移量计算  获取dwg图纸的层号  让后按照顺序把对应的层号拼接起来  然后把拼接好的数据调用C#修改房间的接口

第四步   调用C#质检接口返回ifc文件

第五步    使用SZ-IFC自检工具1.3.1对返回的ifc文件进行检测生成检测报告
